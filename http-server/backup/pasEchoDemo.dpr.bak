program pasEchoDemo;

uses
  Forms, Interfaces,
  TestForm in 'TestForm.pas' {Form2};

{$R *.res}

begin
  Application.Title:='pasEchoDemo';
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
